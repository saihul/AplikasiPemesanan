<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikasipemesanan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiPemesanan">
        <activity android:name=".activities.DataPembayaranActivity"></activity>
        <activity android:name=".activities.DataPengeluaranActivity" />
        <activity android:name=".activities.DataHistoryActivity" />
        <activity
            android:name=".activities.MenuActivity"
            android:theme="@style/Theme.AplikasiPemesanan.NoActionBar" />
        <activity
            android:name=".activities.SettingActivity"
            android:theme="@style/Theme.AplikasiPemesanan.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AplikasiPemesanan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>